var _class;
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return typeof key === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (typeof input !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (typeof res !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
import { zlogerror, zloginfo } from '../utils/logger';
import ZegoPluginResult from './defines';
import ZegoUIKitCorePlugin from "../../components/internal/ZegoUIKitCorePlugin";
export default class ZegoPluginUserInRoomAttributesCore {
  constructor() {
    _defineProperty(this, "_isJoinRoom", false);
    _defineProperty(this, "_roomBaseInfo", {});
    // { roomID: '', roomName: '' }
    _defineProperty(this, "_onUsersInRoomAttributesUpdatedCallbackMap", {});
    if (!ZegoPluginUserInRoomAttributesCore.shared) {
      ZegoPluginUserInRoomAttributesCore.shared = this;
    }
    return ZegoPluginUserInRoomAttributesCore.shared;
  }
  static getInstance() {
    if (!ZegoPluginUserInRoomAttributesCore.shared) {
      ZegoPluginUserInRoomAttributesCore.shared = new ZegoPluginUserInRoomAttributesCore();
    }
    return ZegoPluginUserInRoomAttributesCore.shared;
  }
  // ------- internal events register ------
  _registerEngineCallback() {
    ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance().on('roomMemberAttributesUpdated', (zim, _ref) => {
      let {
        roomID,
        infos,
        operatedInfo
      } = _ref;
      zloginfo(`[ZegoPluginUserInRoomAttributesCore]NotifyUsersInRoomAttributesUpdated`, infos, operatedInfo);
      this._notifyUsersInRoomAttributesUpdated({
        infos: infos.map(info => info.attributesInfo),
        editor: operatedInfo.userID
      });
    });
    zloginfo('[ZegoPluginUserInRoomAttributesCore]Register callback for ZIM...');
  }
  _unregisterEngineCallback() {
    zloginfo('[ZegoPluginUserInRoomAttributesCore]Unregister callback from ZIM...');
    ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance().off('roomMemberAttributesUpdated');
  }
  // ------- internal utils ------
  _resetData() {
    this._resetDataForLeaveRoom();
  }
  _resetDataForLeaveRoom() {
    this._roomBaseInfo = {};
    this._isJoinRoom = false;
  }
  // ------- internal events exec ------
  _notifyUsersInRoomAttributesUpdated(notifyData) {
    Object.keys(this._onUsersInRoomAttributesUpdatedCallbackMap).forEach(callbackID => {
      if (this._onUsersInRoomAttributesUpdatedCallbackMap[callbackID]) {
        this._onUsersInRoomAttributesUpdatedCallbackMap[callbackID](notifyData);
      }
    });
  }
  // ------- external method ------
  joinRoom(roomID) {
    if (!ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance()) {
      zlogerror('[ZegoPluginUserInRoomAttributesCore]Please initialize it first.');
      return Promise.reject();
    }
    return new Promise((resolve, reject) => {
      if (!this._isJoinRoom) {
        ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance().enterRoom({
          roomID,
          roomName: roomID
        }).then(_ref2 => {
          let {
            roomInfo
          } = _ref2;
          zloginfo(`[ZegoPluginUserInRoomAttributesCore]Join the room successfully.`);
          this._roomBaseInfo = roomInfo.baseInfo;
          this._isJoinRoom = true;
          resolve(new ZegoPluginResult('', ''));
        }).catch(error => {
          zlogerror(`[ZegoPluginUserInRoomAttributesCore]Failed to join the room, code: ${error.code}, message: ${error.message}`);
          reject(error);
        });
      } else {
        zloginfo('[ZegoPluginUserInRoomAttributesCore]Join room already success.');
        resolve(new ZegoPluginResult('', ''));
      }
    });
  }
  leaveRoom() {
    if (!ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance()) {
      zlogerror('[ZegoPluginUserInRoomAttributesCore]Please initialize it first.');
      return Promise.reject();
    }
    if (!this._roomBaseInfo.roomID) {
      zlogerror('[ZegoPluginUserInRoomAttributesCore]Please join the room first.');
      return Promise.reject();
    }
    return new Promise((resolve, reject) => {
      ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance().leaveRoom(this._roomBaseInfo.roomID).then(() => {
        zloginfo(`[ZegoPluginUserInRoomAttributesCore]Leave the room successfully.`);
        this._resetDataForLeaveRoom();
        resolve(new ZegoPluginResult('', ''));
      }).catch(error => {
        zlogerror(`[ZegoPluginUserInRoomAttributesCore]Failed to leave the room, code: ${error.code}, message: ${error.message}`);
        reject(error);
      });
    });
  }
  getRoomBaseInfo() {
    return this._roomBaseInfo;
  }
  setUsersInRoomAttributes(attributes, userIDs) {
    if (!ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance()) {
      zlogerror('[ZegoPluginUserInRoomAttributesCore]Please initialize it first.');
      return Promise.reject();
    }
    return new Promise((resolve, reject) => {
      ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance().setRoomMembersAttributes(attributes, userIDs, this._roomBaseInfo.roomID, {
        isDeleteAfterOwnerLeft: true
      }).then(_ref3 => {
        let {
          roomID,
          infos,
          errorUserList
        } = _ref3;
        zloginfo(`[ZegoPluginUserInRoomAttributesCore]Set attributes of users in room successfully.`);
        resolve({
          ...new ZegoPluginResult('', ''),
          errorUserList,
          infos
        });
      }).catch(error => {
        zlogerror(`[ZegoPluginUserInRoomAttributesCore]Failed to set the user's attributes, code: ${error.code}, message: ${error.message}`);
        reject(error);
      });
    });
  }
  queryUsersInRoomAttributes(config) {
    if (!ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance()) {
      zlogerror('[ZegoPluginUserInRoomAttributesCore]Please initialize it first.');
      return Promise.reject();
    }
    return new Promise((resolve, reject) => {
      ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance().queryRoomMemberAttributesList(this._roomBaseInfo.roomID, config).then(_ref4 => {
        let {
          roomID,
          infos,
          nextFlag: resNextFlag
        } = _ref4;
        zloginfo(`[ZegoPluginUserInRoomAttributesCore]Query attributes of users in room successfully.`);
        const params = {
          ...new ZegoPluginResult('', ''),
          nextFlag: resNextFlag,
          infos
        };
        resolve(params);
      }).catch(error => {
        zlogerror(`[ZegoPluginUserInRoomAttributesCore]Failed to query the user's attributes, code: ${error.code}, message: ${error.message}`);
        reject(error);
      });
    });
  }
  // ------- external events register ------
  onUsersInRoomAttributesUpdated(callbackID, callback) {
    if (!ZegoUIKitCorePlugin.getZIMPlugin().default.getInstance()) {
      zlogerror('[ZegoPluginUserInRoomAttributesCore]Please initialize it first.');
    }
    if (typeof callback !== 'function') {
      if (callbackID in this._onUsersInRoomAttributesUpdatedCallbackMap) {
        zloginfo('[Core][onUsersInRoomAttributesUpdated] Remove callback for: [', callbackID, '] because callback is not a valid function!');
        delete this._onUsersInRoomAttributesUpdatedCallbackMap[callbackID];
      }
    } else {
      this._onUsersInRoomAttributesUpdatedCallbackMap[callbackID] = callback;
    }
  }
}
_class = ZegoPluginUserInRoomAttributesCore;
_defineProperty(ZegoPluginUserInRoomAttributesCore, "shared", void 0);
//# sourceMappingURL=user_in_room_attributes_core.js.map